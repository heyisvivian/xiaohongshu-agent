# samples/ —— 你亲手写的笔记

这里放**已发布**的小红书笔记。它有双重身份：

1. **语气语料** —— 只有亲写目录（guide/ life/ zhongcao/）参与语气统计
2. **互动数据层** —— 全部目录（含 `_ai/`）的 frontmatter「互动:」行，
   是 `profile/account.md` 台账的原始数，循环⑤回流就落在这里

## ⚠️ 这个目录**会**上传到 GitHub

2026-08-21 起 `samples/` 进 git（之前是 gitignore 的）。仓库是**公开**的。

放进来的东西是**已经发布过**的笔记，所以原文本来就是公开的 —— 这是这个决定成立的前提。
但它同时改掉了一条规则：

| | 以前（gitignore） | 现在（进 git） |
|---|---|---|
| 已发布的笔记 | ✅ | ✅ |
| **没发布的草稿** | ✅ 随便放 | ❌ **不要放** —— 会被公开 |
| **别人写的东西** | ✅ 参考用 | ❌ 不要放 |
| 站内私信、评论区里别人的信息 | ⚠️ | ❌ |

产出草稿走 `drafts/`，那个目录**仍然** gitignore。

---

## 🛑 只放你自己写的

**AI 生成或 AI 起草的稿子不要放进来。**

拿 AI 稿学"你的语气"，学到的是那个 AI 的语气。然后它写出来的更像通稿，
你又把这些稿子当语料喂回来 —— **几轮之后你的账号就是纯 AI 味，
而且是自己一步步喂出来的。**

放进来之前问一句：**这篇的句子是我自己敲的吗？**

| 情况 | 放不放 |
|---|---|
| 从头到尾自己写的 | ✅ 放 |
| 自己写完自己改的 | ✅ 放 |
| AI 起草 + 你大改（面目全非） | ⚠️ 可以，但文件名标一下 `ai-draft-xxx.md` |
| AI 起草 + 你小改（顺一顺） | ❌ 别放 |
| 全部 AI 生成 | ❌ 别放 |

AI 稿想留着参考就放 `samples/_ai/` —— 下划线开头，统计脚本不会去读那个目录。
（隔离的只是**语气统计**；它们的互动数据照样进 account.md 台账 ——
能源支票藏率 115% 正是台账里最重要的样本之一。）

### 如果某类内容你一向是 AI 起草

那这类内容的档案换个做法：**不是"提取你的语气"，是"定义你希望它读起来什么样"。**
跟 agent 说清楚你的实际工作方式，它会改用问答的方式建这一节
（问你改 AI 稿时最常删什么、哪种句子看到就想改）。

---

## ★ 按内容类型分子目录

这个账号有三种内容（A 种草 / B 攻略 / C 生活），写法不是同一套，所以**分开放、分开统计**：

```
samples/
├── guide/     攻略 / 信息型（法规解读、清单、干货、测评）
├── life/      生活 / 日常 / 异国恋 / 吐槽
├── zhongcao/  种草 / 好物（带货稿文件名加 ad- 前缀）
├── _ai/       AI 写的（不进语气统计；互动数据照进台账）
├── _partial/  正文不全的（补全后移出）
└── _video/    零文案视频（没有语气可学，数据照记）
```

```bash
python skills/xhs-voice/scripts/voice_stats.py samples/guide
python skills/xhs-voice/scripts/voice_stats.py samples/life
python skills/xhs-voice/scripts/voice_stats.py samples/zhongcao
```

**混在一起统计没意义** —— 不同模式的指标会被平均成一个不存在的数字。
攻略稿 emoji 当评分刻度（💣🌟 可连排），种草稿只在行末放一个，
平均出来的密度两边都不对；A1 种草稿零标点，混进去句长统计直接失真。

只有一种内容就直接放 `samples/` 根目录。
第三种类型（比如恰饭稿、测评）就再开一个子目录。

---

## 怎么放

一篇一个文件，`.md` 或 `.txt` 都行。文件名随便。

推荐格式（带 frontmatter，统计脚本能识别标题）：

```markdown
---
title: 巴黎 | 每年的例行常规体检攻略
tags: [体检, 法国生活, 医疗]
线: 法国生活实操
互动: 赞 158 · 收藏 191 · 评论 31 · 分享 57
备注: 收藏 > 点赞，典型攻略型互动形状
---
只要你有那个 Ameli 卡都可以免费预约去做的
……
```

「线」「互动」「备注」是数据字段：**「互动:」行是循环⑤的落点** ——
发布 D+7 回填，之后每次复盘直接读它（详见 `profile/account.md` 回流节奏）。

**懒得加 frontmatter 也行** —— 直接粘贴正文，第一行会被当成标题。

格式参考 `_format-example.md`。

⚠️ 那个文件名以**下划线开头**是故意的 —— 下划线开头的文件和目录
`voice_stats.py` 一律跳过，所以它不会被当成你的语气样本。
你自己的笔记用正常文件名（不带下划线）。

---

## 怎么导出

小红书没有批量导出。都得手动，几种方式：

1. **App 里复制** —— 我的 → 笔记 → 打开 → 长按正文复制 → 粘贴进新文件
2. **创作者后台** —— 电脑上打开 creator.xiaohongshu.com，笔记管理里复制
3. **你本来的草稿** —— 如果你在备忘录/Notion 里写过草稿，直接拿过来

**agent 现在可以帮你读**（2026-08 放开，见 `AGENTS.md` 1.5）—— 但只在你登录态下读你自己的，
碰到验证码就停。最稳的还是创作者后台导出；手动贴 15 篇也就十几分钟。

---

## 放几篇

| 篇数 | 效果 |
|---|---|
| **15+** | 统计稳定，档案能做得很准 ★ |
| 8–14 | 够用，少数特征会不稳 |
| 3–7 | 只能抓大方向，档案会标「样本不足」 |
| 0 | 走问卷冷启动（`skills/xhs-voice/references/questionnaire.md`） |

---

## 挑哪些篇

**第一道筛：是不是你自己写的。**（见上面那节，这条不能让）

**第二道筛：挑你自己满意的**，而不是数据最好的。

写得不像你的那几篇（比如硬着头皮写的恰饭稿、照模板套的攻略）会**污染档案** ——
学出来的语气会往那个方向偏。

如果某几篇你自己都觉得「这不像我写的」，别放进来。

有恰饭稿的话可以单独放，但**文件名里标一下**（比如 `ad-xxx.md`），
这样档案里能区分「日常语气」和「恰饭语气」。

---

## 放完之后

```
学一下我的语气
```

或者直接跑统计看看：

```bash
python skills/xhs-voice/scripts/voice_stats.py samples/guide
python skills/xhs-voice/scripts/voice_stats.py samples/life
python skills/xhs-voice/scripts/voice_stats.py samples/zhongcao
```

会输出句长分布、标点频率、emoji 偏好、口头禅、开头结尾套路。
然后模型结合原文生成 `profile/voice.md`。

---

## 之后

每积累 **10 篇新笔记**，把它们也放进来重跑一次 —— 你的风格会变，档案要跟着更新。
